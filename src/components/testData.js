import React from 'react'
import axios from 'axios'

const api = axios.create({
    baseURL:''
})

const testData = () => {
    return (
        <div>
            
        </div>
    )
}

export default testData
